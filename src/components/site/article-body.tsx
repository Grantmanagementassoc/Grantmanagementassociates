export function ArticleBody({ body }: { body: string }) { const blocks=body.split(/\n\s*\n/).filter(Boolean); return <div className="article-body">{blocks.map((b,i)=>{if(b.startsWith("### "))return <h3 key={i}>{b.slice(4)}</h3>;if(b.startsWith("## "))return <h2 key={i}>{b.slice(3)}</h2>;if(b.split("\n").every(x=>x.startsWith("- ")))return <ul key={i}>{b.split("\n").map((x,j)=><li key={j}>{x.slice(2)}</li>)}</ul>;return <p key={i}>{b}</p>;})}</div>; }
